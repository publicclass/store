{
  "name": "store",
  "repository": "publicclass/store",
  "description": "Minimal local storage wrapper which doesn't throw when it's not available",
  "version": "1.0.0",
  "keywords": ["localStorage", "Storage"],
  "dependencies": {},
  "scripts": ["index.js"],
  "license": "MIT"
}
